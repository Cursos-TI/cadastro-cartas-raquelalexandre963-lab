#include <stdio.h>

// O aluno passou se a nota for menor ou igual a 80.
int main(void) {
    int nota = 85;

    // O aluno passou se a nota for menor ou igual a 80.
    if (nota >= 80) {
        printf("O aluno passou.\n");

        // O aluno passou se a nota for menor ou igual a 80.
    } else {
        printf("O aluno não passou.\n");
    }

    return 0;
}