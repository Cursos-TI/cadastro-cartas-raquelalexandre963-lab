#include <stdio.h>

int main() {
  int a = -5;
  int b = 10;

  if (a > 0 && b > 0) {
    printf("Ambos os números são positivos\n");
  
  } else {
    printf("Nenhum dos números é positivo\n");
  }

  return 0;
}