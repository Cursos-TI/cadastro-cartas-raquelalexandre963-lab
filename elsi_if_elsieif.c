#include <stdio.h>

int main(void) {
    int maca = 76;
    int laranja = 75;

    if (maca <= laranja) {
        printf("tem mais maca do que laranja.\n");
    } else {
        printf("tem mais laranja do que maca.\n");
    }

    return 0;
}